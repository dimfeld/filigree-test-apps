#![allow(unused_imports, unused_variables, dead_code)]
use super::{PostTagCreatePayload, PostTagUpdatePayload};
use crate::models::{post::PostId, tag::TagId};

/// Generate a PostTagCreatePayload for testing.
/// Parameter `i` controls the value of some of the fields, just to make sure that the objects
/// don't all look identical.
pub fn make_create_payload(i: usize) -> PostTagCreatePayload {
    PostTagCreatePayload {
        post_id: None,

        tag_id: None,
    }
}

/// Generate a PostTagUpdatePayload for testing.
/// Parameter `i` controls the value of some of the fields, just to make sure that the objects
/// don't all look identical.
pub fn make_update_payload(i: usize) -> PostTagUpdatePayload {
    PostTagUpdatePayload {
        post_id: None,

        tag_id: None,
    }
}
